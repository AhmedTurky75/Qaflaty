export const environment = {
  production: true,
  apiUrl: 'https://api.qaflaty.com/api',
  appName: 'Qaflaty Merchant Dashboard',
  // Origin where the STORE app is served for the live preview iframe. Leave
  // empty to derive it from the store's slug / custom domain
  // (https://{slug}.qaflaty.com). Set an explicit origin if the storefront is
  // served from a fixed host instead.
  storeBaseUrl: ''
};
